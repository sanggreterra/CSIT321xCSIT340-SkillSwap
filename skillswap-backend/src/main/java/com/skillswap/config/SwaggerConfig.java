package com.skillswap.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Intentionally left minimal. Add OpenAPI / Swagger configuration here if desired.
}
